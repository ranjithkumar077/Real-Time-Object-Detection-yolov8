# YOLOv8 Model Files

The application uses `yolov8n.pt` by default. Ultralytics downloads it automatically on the first run if it is not already available.

You can also upload a custom `.pt` model from the dashboard. Uploaded models are stored in this folder and can be used immediately.
